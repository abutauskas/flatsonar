# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
dist/

# Local state
server/.env
*.db
*.db-shm
*.db-wal
*.sqlite3
server/data/
.crawl-cache/
# The crawled catalogue IS the published site's data and has to survive between CI
# runs (first_seen dates, the Atom feed, id-collision history) or a fresh crawl
# would relist everything as "new" every time; track it deliberately.
!/server/flatsonar.db

# Scratch (previews, local test builds)
.servetest/
.buildtest/
public/

# flatpak-builder
.flatpak-builder/
build-dir/
repo/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
